{{ set(this, 'title', 'Create Item') }}
{{ set(this, 'params', { 'breadcrumbs' : [
	{ 'label' : model.menu.title, 'url' : path('menu/view', { 'id' : model.menu.id }) },
	{ 'label' : 'Items', 'url' : path('index', { 'menuId' : model.menu.id }) },
	{ 'label' : 'Create' }
	]}) }}

<div class="menu-create">
	
	{{ this.render('_form.twig', {
		'model' : model,
		'list' : list,
		'uploadForm' : uploadForm
	}) | raw }}

</div>
