{{ set(this, 'title', 'Create Category') }}
{{ set(this, 'params', { 'breadcrumbs' : [
	{ 'label' : 'categories', 'url' : path('category/index')},
	{ 'label' : this.title }
	]}) }}

<div class="category-create">
	{{ this.render('_form.twig', {
		'model' : model,
		'meta' : meta,
		'list' : list}) | raw
	}}
</div>